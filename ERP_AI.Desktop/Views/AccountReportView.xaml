<UserControl x:Class="ERP_AI.Desktop.Views.AccountReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Account Reports" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <ComboBox ItemsSource="{Binding ReportTypes}" SelectedItem="{Binding SelectedReportType}" Width="300" Margin="0,0,0,10"/>
            <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Width="150" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding ReportData}" AutoGenerateColumns="True" Height="350"/>
        </StackPanel>
    </Grid>
</UserControl>
