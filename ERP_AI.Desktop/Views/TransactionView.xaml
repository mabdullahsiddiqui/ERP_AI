<UserControl x:Class="ERP_AI.Desktop.Views.TransactionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Journal Entries" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <Button Content="Add Transaction" Command="{Binding AddTransactionCommand}" Width="150" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Transactions}" SelectedItem="{Binding SelectedTransaction}" AutoGenerateColumns="True" Height="150" Margin="0,0,0,10"/>
            <TextBlock Text="Transaction Details" FontWeight="Bold" Margin="0,10,0,5"/>
            <Button Content="Add Detail" Command="{Binding AddDetailCommand}" Width="120" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Details}" AutoGenerateColumns="True" Height="150" Margin="0,0,0,10"/>
            <Button Content="Post Transaction" Command="{Binding PostTransactionCommand}" Width="150" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
